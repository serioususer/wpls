\section{Bit Error Rate}


\begin{python}
def ber(X, Y):
	hw = 0.0
	for x,y in len(X):
		hw += x^y
	
	BER = hw/len(X)	# len(X) = len(Y)
	
	if 0 <= BER <= 0.5:
		return BER
	else:
		return 1 - BER
\end{python}
